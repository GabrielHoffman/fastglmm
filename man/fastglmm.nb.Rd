% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastglmm.nb.R
\name{fastglmm.nb}
\alias{fastglmm.nb}
\title{Fit Negative Binomial Mixed Model via PQL}
\usage{
fastglmm.nb(
  formula,
  data,
  weights = NULL,
  maxit = 100,
  tol = 0.001,
  tol.eta = 0.001,
  doCoxReid = nrow(data) < 1000,
  lambda = 0,
  nthreads = 6
)
}
\arguments{
\item{formula}{a two-sided linear formula object describing both the fixed-effects and random-effects part of the model, with the response on the left of a \code{~} operator and the terms, separated by \code{+} operators, on the right.  Random-effects terms are distinguished by vertical bars (\code{|}) separating expressions for design matrices from grouping factors.}

\item{data}{an optional data frame containing the variables named in}

\item{weights}{an optional vector of prior weights with a value for each sample.}

\item{maxit}{max number of NB iterations}

\item{tol}{convergence criterion for the 1D search of the delta space}

\item{tol.eta}{convergence criterion \code{eta} in the PQL iteration}

\item{doCoxReid}{use Cox-Reid correction for estimating theta in negative binomial model}

\item{lambda}{ridge shrinkage parameter}

\item{nthreads}{number of threads}
}
\description{
Fit negative binomial mixed model (GLMM) with a single random effect using penalized quasi-likelihood (PQL)
}
\examples{
library(MASS)
data(PsychAD)

# regression formula
form <- PTPRG ~ (1|SubID) + offset(log(libSize))

# NB GLMM on PTPRG expression via PQL
fit1 <- fastglmm.nb(form, PsychAD)
coef(summary(fit1))

# NB GLMM via Laplace approximation
# fit2 <- lme4::glmer.nb(form, PsychAD)
# coef(summary(fit2))
}
