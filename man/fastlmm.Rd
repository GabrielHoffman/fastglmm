% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastlmm.R, R/fastlmm.fit.R
\name{fastlmm}
\alias{fastlmm}
\title{Fast Linear Mixed Model with 1 random effect}
\usage{
fastlmm(
  formula,
  data,
  REML = TRUE,
  delta = NULL,
  weights = NULL,
  delta.range = c(-10, 10),
  tol = .Machine$double.eps^0.5,
  nthreads = 6
)
}
\arguments{
\item{formula}{a two-sided linear formula object describing both the fixed-effects and random-effects part of the model, with the response on the left of a \code{~} operator and the terms, separated by \code{+} operators, on the right.  Random-effects terms are distinguished by vertical bars (\code{|}) separating expressions for design matrices from grouping factors.}

\item{data}{an optional data frame containing the variables named in}

\item{REML}{logical scalar - Should the estimates be chosen to optimize the REML criterion vs ML?}

\item{delta}{if \code{NULL} estimate delta, if value is given use this fixed value}

\item{weights}{an optional vector of prior weights with a value for each sample.  When the response has multiple columns, a vector of weight can be reused for each respose, or a matrix the same dimension as the responses matrix can weight each response separately.}

\item{delta.range}{min and max values (in log space), of the search space for delta to fit the random effect}

\item{tol}{convergence criterion for the 1D search of the delta space}

\item{nthreads}{number of threads}
}
\description{
Fit a linear mixed-effects model with 1 random effect with REML or maximum likelihood using the very fast algorithm and implementation

Description
}
\details{
\insertCite{hoffman2013}{fastlmm} and \insertCite{lippert2011}{fastlmm}
}
\examples{
library(lme4)

fit <- fastlmm(Reaction ~ Days + (1 | Subject), sleepstudy)

fit

summary(fit)

# results are identical for lmer(...,REML=FALSE)
fit2 <- lmer(Reaction ~ Days + (1 | Subject), sleepstudy, REML=FALSE)
coef(summary(fit2))

}
\references{
\insertAllCited{}
}
\seealso{
\code{lme4::lmer()}
}
