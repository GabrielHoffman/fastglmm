% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{lmFitResponses}
\alias{lmFitResponses}
\title{Fit series of linear regression models to multiple responses with shared design matrix}
\usage{
lmFitResponses(Y, X, ids, Weights, detail = 0L, nthreads = 1L)
}
\arguments{
\item{Y}{matrix of responses as columns}

\item{X}{design matrix}

\item{ids}{vector<string> storing identifier for each column in Y}

\item{Weights}{matrix sample-level weights the same dimension as Y}

\item{detail}{level of model detail returned, with LOW = 0, MEDIUM = 1, HIGH = 2. LOW (\code{beta}, \code{se}, \code{sigSq}, \code{rdf}), MEDIUM (\code{vcov}), HIGH (\code{residuals}), MOST (\code{hatvalues})}

\item{nthreads}{number of threads.  Each model is fit in serial, analysis is parallelized across responses.}
}
\value{
List of parameter estimates with entries \code{coef},  \code{se}, \code{sigSq}, \code{rdf} and other depending on \code{detail}
}
\description{
Fit regression model \code{Y[,j] ~ X_design} for each feature j
}
\details{
Since the weights vary for each response, each model is computed separately without recycling precomputed values
}
\examples{
n = 100
m = 5
nc = 2
set.seed(1)
Y = matrix(rnorm(n*m), n, m)
X = matrix(rnorm(n*nc), n,nc)
colnames(Y) = seq(m)
W = matrix(runif(n*m), n,m) 

# fit regressions with model j using Y[,j] as a response
fit = lmFitResponses(Y, X, colnames(Y), W)

# examine results
lapply(fit, head, 2)

}
